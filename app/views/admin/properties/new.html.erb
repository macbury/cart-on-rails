<h1>New property</h1>

<% semantic_form_for([:admin, @property]) do |f| %>
  <% f.inputs do %>
		<%= f.input :presentation %>
		<%= f.input :name %>
	<% end %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_properties_path %>