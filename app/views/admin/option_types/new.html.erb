<h1>New option_types</h1>

<% semantic_form_for([:admin, @option_type]) do |f| %>
	<% f.inputs do %>
		<%= f.input :presentation %>
		<%= f.input :name %>
	<% end %>
	
	<% f.inputs :name => "Wartości" do %>
		<% for option in @option_type.option_values %>
			<% f.inputs :for => [:option_values, option] do |option_form| %>
				<%= option_form.input :name %>
			<% end %>
		<% end %>
	<% end %>
	
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_option_types_path %>